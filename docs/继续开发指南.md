# 在其他地方继续开发本工具

代码已托管到 GitHub，与本地机器解耦。任何一台电脑，clone 下来就能改，push 即同步。

## 仓库

- **地址**：https://github.com/chj0124/spec-decision
- **默认分支**：`main`
- **可见性**：Public（如需私有，到仓库 Settings → Danger Zone → Change visibility）

---

## 一、在新电脑上开始

```bash
# 1. 克隆
git clone https://github.com/chj0124/spec-decision.git
cd spec-decision

# 2. 配置 git 身份（首次）
git config user.name "chj0124"
git config user.email "chj0124@users.noreply.github.com"

# 3. 国内环境配代理（GitHub 被墙时）
git config http.proxy http://127.0.0.1:7897
git config https.proxy http://127.0.0.1:7897

# 4. 装依赖（国内用淘宝镜像更快）
npm install --registry=https://registry.npmmirror.com

# 5. 起开发服务器
npm run dev        # → http://localhost:5173
```

需要 Node 18+（推荐 22）。没装的话先去 https://nodejs.org 装 LTS 版。

---

## 二、日常开发流程

```bash
# 改代码...
npm run dev          # 边改边看

# 提交前验证
npm run build        # tsc 类型检查 + vite 构建，必须通过
npm test             # vitest 单元测试
npm run e2e          # Playwright 冒烟（构建后跑，桌面 + 手机双视口）

# 提交 + 推送
git add -A
git commit -m "feat: 描述这次改动"
git push
```

**铁律**：`git push` 前一定先 `npm run build` 跑通，避免把编译不过的代码推上去。

**版本号**：页脚版本号由 vite 从 `package.json` 的 `version` 构建期注入，e2e 动态读取断言；有功能改动时记得 bump（功能新增 → 次版本号）。

---

## 三、部署（双轨，任选其一）

### A. Cloudflare Workers（当前在线版）

```bash
npm run build
npx wrangler deploy   # wrangler.toml 已配置：_worker.js 接管 /api/* + 静态资源 SPA 回退
```

在线地址：<https://spec-decision.1663884773.workers.dev/>

### B. Vercel 自动部署（配一次，以后 push 即上线）

只需在 Vercel 网页关联一次，之后每次 `git push` 自动构建发布：

1. 打开 https://vercel.com ，用 GitHub 账号登录
2. **Add New → Project** → 选 `chj0124/spec-decision`
3. Vercel 会自动识别 **Vite** 框架：
   - Build Command：`npm run build`（默认，不用改）
   - Output Directory：`dist`（默认，不用改）
   - 仓库里已有 `vercel.json`，会自动生效
4. 点 **Deploy**，约 1 分钟出正式链接（形如 `spec-decision.vercel.app`）

配好后：
- `git push` → Vercel 自动拉代码 → 构建 → 上线
- 每次 push 生成一个预览链接，`main` 分支对应正式域名

### 环境变量（如需 AI 截图识别走服务端端点；浏览器直连视觉模型不需要）

在 Vercel 项目 **Settings → Environment Variables** 加：

| 变量 | 用途 |
|------|------|
| `DASHSCOPE_API_KEY` | 通义千问视觉模型密钥（Serverless 识别端点用） |
| `RECOGNIZE_PROVIDER` | 识别服务商，默认 `qwen` |

> 用户在网页里配的 AI（单位换算/文案润色）走的是浏览器端配置，不依赖 Vercel 环境变量。

---

## 四、目录结构速查

```
src/
├── App.tsx                 # 入口 / 页面切换 / 多清单 / 主题 / 分享路由
├── components/
│   ├── Workbench.tsx       # 工作台（录入/价格历史/权重/识别与粘贴入口/极速录入）
│   ├── RecognizeReview.tsx # 识别确认页（可编辑/低置信度高亮/替换 or 追加）
│   ├── Report.tsx          # 报告页（排名/主视觉+避坑标注/升档卡/导出分享）
│   ├── AiSettings.tsx      # AI 配置面板
│   ├── ScenarioBar.tsx     # 多清单条（切换/备份）
│   └── WeightPie.tsx       # 权重环形图（内联 SVG）
├── lib/
│   ├── engine/             # 计算引擎（util/units/spec/scoring/clusters/history/decide + 单测）
│   ├── recognize.ts        # 识别逻辑（视觉模型优先→端点兼容→演示兜底）
│   ├── ai.ts               # AI 客户端（OpenAI 兼容）
│   ├── useUnitNormalize.ts # 生僻单位 AI 归一化 hook
│   ├── store.ts            # localStorage 持久化 + 迁移 + 备份
│   ├── share.ts            # 只读分享（URL hash 压缩）
│   └── types.ts            # 领域模型
api/                        # Vercel 函数（recognize / ai-chat / ai-models）
shared/aiProxyCore.js       # AI 代理核心（vite dev / Vercel / Cloudflare 三端共用）
e2e/                        # Playwright 冒烟测试
_worker.js + wrangler.toml  # Cloudflare Workers 部署
```

---

## 五、常见问题

**push 时卡在授权 / 403**
→ 本机 Git Credential Manager 已存凭据可直推；新电脑首次 push 会弹 GitHub 授权窗口，登录即可。国内务必先配代理（见上）。

**npm install 慢 / 失败**
→ 加淘宝镜像 `--registry=https://registry.npmmirror.com`。

**构建报单位/类型错**
→ 先 `npm run build` 看完整报错；本项目单位归一化逻辑在 `src/lib/engine/units.ts`，双向同步（规格描述 ↔ 结构化字段）在 `src/lib/engine/spec.ts`，由 Workbench/RecognizeReview 调用。

**想改但怕搞乱**
→ 开个分支 `git checkout -b my-feature`，改完合并回 `main`；或先 push 到新分支，在 GitHub 上开 PR 对比。
